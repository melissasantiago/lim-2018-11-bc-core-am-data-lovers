require('../src/data.js');


describe('dataClone', () => {
  it('debería ser una función', () => {
    assert.equal(typeof dataClone, 'function');
  });

//  it('returns `example`', () => {
//    expect(example()).toBe('example');
//  });
});
